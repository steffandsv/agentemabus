<%- include('partials/header') %>

<div class="row justify-content-center">
    <div class="col-md-8">
        <h2 class="mb-4">✨ Nova Missão</h2>
        <div class="card shadow">
            <div class="card-body">
                <form action="/create" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label class="form-label">Nome da Tarefa</label>
                        <input type="text" name="name" class="form-control" required placeholder="Ex: Cotação TI - Lote 1">
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">CEP de Destino</label>
                        <input type="text" name="cep" class="form-control" required placeholder="00000-000">
                    </div>

                    <div class="card mb-3 bg-light">
                        <div class="card-body">
                            <h5 class="card-title">Dados de Entrada</h5>
                            <p class="card-text text-muted small">Escolha uma das opções abaixo:</p>
                            
                            <ul class="nav nav-tabs" id="inputTab" role="tablist">
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link active" id="file-tab" data-bs-toggle="tab" data-bs-target="#file-pane" type="button" role="tab">📁 Enviar Arquivo</button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="text-tab" data-bs-toggle="tab" data-bs-target="#text-pane" type="button" role="tab">📝 Colar Texto</button>
                                </li>
                            </ul>
                            
                            <div class="tab-content pt-3" id="inputTabContent">
                                <div class="tab-pane fade show active" id="file-pane" role="tabpanel">
                                    <div class="mb-3">
                                        <label class="form-label">Arquivo CSV</label>
                                        <div class="input-group">
                                            <input type="file" name="csvFile" class="form-control" accept=".csv">
                                            <a href="/download-template" class="btn btn-outline-secondary">⬇️ Baixar Modelo</a>
                                        </div>
                                    </div>
                                </div>
                                <div class="tab-pane fade" id="text-pane" role="tabpanel">
                                    <div class="mb-3">
                                        <label class="form-label">Conteúdo do CSV</label>
                                        <textarea name="csvText" class="form-control" rows="5" placeholder="ID,Descricao&#10;1,Notebook i7&#10;2,Mouse sem fio"></textarea>
                                        <div class="form-text">Cole o conteúdo seguindo o cabeçalho <code>ID,Descricao</code>.</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary">🚀 Decolar (Iniciar)</button>
                        <a href="/" class="btn btn-outline-secondary">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>
